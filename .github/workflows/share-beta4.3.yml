name: Share ProxPanel OTA

on:
  push:
    branches:
      - share/1.7.2-beta.4.3

permissions:
  contents: read

jobs:
  package:
    runs-on: ubuntu-latest
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Download OTA release asset
        shell: bash
        run: |
          set -euo pipefail
          gh release download v1.7.2-beta.4.3 \
            --repo "${{ github.repository }}" \
            --pattern "proxpanel-update-v1.7.2-beta.4.3.zip" \
            --dir out
      - name: Upload OTA artifact
        uses: actions/upload-artifact@v4
        with:
          name: proxpanel-update-v1.7.2-beta.4.3
          path: out/proxpanel-update-v1.7.2-beta.4.3.zip
          if-no-files-found: error
          retention-days: 1
