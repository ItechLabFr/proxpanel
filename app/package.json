{
  "name": "proxpanel",
  "version": "1.7.2-beta.8.3",
  "private": true,
  "description": "ProxPanel 1.7.2-beta.8.3 - fix single-server node hardware enrichment so CPU model data reaches the UI.",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  }
}
